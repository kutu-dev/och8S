sources = files('main.c', 'render.c', 'virtual-machine.c')

    exe = executable('och8S', sources, dependencies
                     : sdl2, include_directories
                     : include_directories('headers'))
